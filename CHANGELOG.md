# 更新日志

## [1.5.0] - 2026-08-13

### 新增
- 「沙箱脱壳（免root）」：选 APK 自动装入虚拟沙箱静默运行并 dump dex，全程免root；完成后自动打包 zip 导出到 Download 并清理临时文件
- 内置 BlackBox 虚拟沙箱引擎：免重打包直接运行原始 APK，规避签名校验/完整性校验
- 沙箱静默运行：只拉起进程不显示界面；脱壳结束自动卸载沙箱内应用释放空间
- 沙箱页新增「一键清理沙箱」按钮，批量卸载沙箱内已装应用
- 加固特征识别：选 APK 时自动识别加固方案（360、梆梆、爱加密、网易易盾等 40+ 种）并提示
- 32 位 ARM 应用支持（放宽 ABI 检查，按设备能力判断）
- 线程名伪装 / 系统属性伪装（反沙箱环境检测）

### 优化
- 脱壳进度：60s 倒计时 + 每 10s 提示 + 完整分步骤日志
- 脱壳触发优化：before/after Application 双触发 + 立即同步 dump，更早抓到已加载 dex
- 沙箱环境检测对抗基础：进程名(/proc cmdline)重定向、属性伪造

### 修复
- 无 root 设备启动不再弹 root 权限提示
- 应用行为控制/内存脱壳/MK热修复页面在无 root 设备上的崩溃（mDir 空值防护）
- 依赖冲突：FreeReflection 统一为 3.2.2（经 Bcore 传递）

## [1.4.9] - 2026-08-12

### 新增
- 方法返回值探测：在「应用行为控制 → 自定义」中可配置类名/方法名，沙箱进程内 hook 指定方法并记录返回值（类型、布尔真假、字符串、时间戳毫秒/秒智能判断等）
- 探测结果保存后即时刷新列表显示「已配置 N 个方法」，不再显示未配置

### 优化
- 方法返回值探测加入 500ms 限流：同一方法两次记录最小间隔，避免高频方法（如 `System.currentTimeMillis`）拖垮目标应用启动

### 修复
- 保存探测配置后详情页未刷新，仍显示「未配置」的问题

## [1.4.8] - 2026-08-10

### 优化
- 内存脱壳：新增多批次自动枚举（3/8/15/25/40/60 秒），可抓延迟解密的加固壳
- 脱壳列表新增「二次脱壳」按钮：手动触发目标进程重新枚举已加载 dex（配合 dump_now 标志）
- 脱壳列表显示已脱壳数量与大小（N 个 dex / xx MB）及保存路径
- 目标应用运行检测改用 su `pidof`（`getRunningAppProcesses` 在 Android 8+ 仅能看到本应用进程，导致误判）

### 修复
- 应用行为控制数据不稳定：SQLite 并发写加 `busy_timeout`、`insert` 失败自动重试、事件缓冲就绪即冲刷，避免数据间歇丢失

## [1.4.7] - 2026-08-10

### 优化
- 内存脱壳重写读取策略：不再 hook `loadClass` 频繁扫内存（易触发加固崩溃），改为延迟后台枚举已加载 dex，按 `DexFile.mCookie` 的 begin/size 精确定位读取（BlackDex 思路）
  - 对带反作弊加固的应用（如阿里 Ashield）也能完整 dump 出 dex（实测疯狂刷题 3 个 dex 全部落盘）
- 脱壳界面新增 dex 保存位置提示：列表项常驻显示 `/data/mHook/<包名>/dump/`，开启弹窗与 Toast 同步提示

### 修复
- 内存脱壳新建 dump 目录权限为 700 导致目标进程无法写入：改为 su 创建即 777
- 脱壳产物 dex 文件权限为 600 导致 mHook 无法读取：写入后置为 666 可读

### 致谢
- 「感谢开源项目」新增 dexlib2（JesusFreke）、Guava（Google）、BlackDex（CodingGay）

## [1.4.6] - 2026-08-10

### 新增
- 检查更新：接入 GitHub Releases API，启动自动检测 + 设置页手动检查；发现新版弹窗展示版本与更新日志
  - 弹窗支持「暂不更新 / 不再提示 / 下载更新」，点「下载更新」跳转浏览器下载安装
  - 启动自动检测支持「不再提示」忽略某版本，手动检测不受忽略影响
- 打赏支持：新增收款码界面，展示支付宝 / 微信收款码，文案「请夏糜吃鸡腿」
- AI 设置新增「连接测试」按钮（一键验证接口连通性）与「最大工具调用轮数」配置

### 优化
- 主页「AI 分析」更名「应用分析」
- XP模块分析AI版：选中 APK 后自动用 dexlib2 提取并展示 hook 点与混淆评估，由用户决定是否再交 AI 分析
- 多应用 AI 结果解析容错：自动跳过 hooks 为空的条目，不再因个别应用无配置导致整体解析失败

### 修复
- 自定义 FileProvider 在部分环境多 dex 类加载崩溃，改用内置 BuglyFileProvider 服务安装包

## [1.4.5] - 2026-08-09

### 新增
- 主页新增「XP模块分析AI版」：选择 XP 模块 APK → 设备端 dexlib2 提取 hook 点 → 调用 AI 生成多应用 Hook 配置并一键导入，实时显示分析过程
- 混淆识别：对字符串加密/控制流混淆的 XP 模块自动检测并明确提示，避免无效分析
- 仓库新增独立提取工具 `tools/ExtractHooks.java`（纯 dexlib2，供 AI 分析使用）

### 优化
- 主页「AI 分析」更名「应用分析」，与 XP 模块分析区分
- 移除旧的解释器版「XP模块分析」入口，统一保留 AI 版

### 修复
- XP 模块分析严重漏报：装箱返回值（`new Integer(1)`/`new Boolean(true)`/`new Long(...)` 等）未在回调指令流中解析，导致仅识别 4 条 hook；现已完整解析（该样本 70/70 条全部还原）

## [1.4.4] - 2026-08-07

### 新增
- 主页新增「AI 分析」入口（第 5 位），一键进入 AI 分析/自动改包

### 优化
- 移除「自定义HOOK」BoomMenu 中的 AI 分析入口（与主页入口重复）

## [1.4.3] - 2026-08-06

### 修复
- 高版本安卓（7.0+）热修复加载失败：ART 只允许从只读文件加载 dex，现补丁 dex 解压后置为只读，修复 dex 合并热修复在高版本安卓上无法生效的问题

## [1.4.2] - 2026-08-06

### 新增
- 设置页新增「感谢开源项目」开源致谢（含玄星逆核技能文档等）
- AI「生成修复包」改为调用 MT 管理器 MCP 直接定位、修改并构建签名 APK，不再依赖本地 mk 工具

### 优化
- 生成修复包时自动复用首次分析的结论（已定位的修改方法及位置），避免 AI 从头分析，节省 Token

### 修复
- MCP 设置页条目详情文字被裁剪
- AI 页按钮间距不一致

## [1.4.1]

- 阶段一：AI 分析生成 Hook 配置与热修复补丁
