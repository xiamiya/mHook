## [2.1.1] - 2026-09-26

### 新增
- 新增「eBPF 深度调试」（主页 → 动态分析，需 root）：内置 eBPF 脱壳引擎，将 uprobe 挂到 libart 解释器/Nterp/生命周期上，在内核态被动抓取应用"执行过"的 dex 与方法字节码，退出时回填被抽取的方法
  - 目标选择使用已安装应用列表（与内存分析同一套选择器，支持搜索）
  - 抓取期间用系统 Toast 提示剩余时间，状态栏同步进度
  - 抓取结束后自动检测去重 + 合并修复 + 打包导出 zip
- 新增 DEX 合并修复：按类描述符去重后，用 dexlib2 重建为尽量少的 dex；受 DEX 各索引池 64K 上限约束时自动二分重卷，输出 classes.dex / classes2.dex …，并修复 header/checksum/signature
- 感谢开源项目页补全分类清单（核心框架 / 过签加固 / 脱壳参考 / Native Hook / Dex 与文件处理 / UI 基础库）

### 修复
- 修复加固强度检测日志累计超过阈值后被整段清空的问题，改为滑动窗口保留（不再丢失前面的记录）

### 优化
- 加固强度检测标题显示授权有效期（如 加固强度检测(2026.9.26 22:07)），单行自适应字号
- 精简分析日志：去掉逐 dex 的刷屏明细、SR 方案 config 全文字段，只保留关键进度
- 步骤 6 反自毁补丁增加逐 dex 进度输出，避免长时间无回显被误认为卡死
- DEX 合并过程每 5 秒输出进度（已扫描/已写入/已分卷），并在 logcat 打点便于排查
- 分析日志支持长按复制